using System.Text;

namespace Cppia.NET.Std;

// https://api.haxe.org/StringBuf.html
public class StringBuffer
{
    // public int Length => _builder.Length;
    
    // private readonly Cppia

    // public void Add(object value) => _builder.Append(value);

    // public void AddChar(int c) => _builder.Append((char)c);

    // public void AddSub(string s, int pos, int? len)
    // {
    //     string value;
    //     if(len is null || !len.HasValue)
    //         value = s[pos..];
    //     else
    //         value = s.Substring(pos, len.Value);
    //     _builder.Append(value);
    // }

    // public override string ToString() => _builder.ToString();
}
